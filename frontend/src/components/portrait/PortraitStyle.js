import styled from "styled-components";

const InfoContainer = styled.div`
    margin-top: 1px;
`;

const Nome = styled.p`
    font-weight: bold;
    text-align: center;
`;

const Linha = styled.div`
    display: flex;
    justify-content: space-between;
    margin-bottom: 5px;
`;

export { InfoContainer, Nome, Linha };